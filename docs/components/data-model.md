# Data Model — HearthVTT (`docs/components/data-model.md`)

> **⚠️ THIS DOCUMENT IS TBD — DO NOT IMPLEMENT**
>
> This document is a placeholder. The data model is not yet defined.
>
> If you are an agent asked to implement anything in this document:
>
> 1. **STOP** — Do not hallucinate schemas or file formats.
> 2. Create an ADR in `docs/decisions/` documenting the decisions you would need to make.
> 3. Ask for human review before proceeding.
>
> For canonical type definitions, see [shared-types.md](../shared-types.md).

This document defines the core data structures, file formats, and schemas used throughout HearthVTT.

---

## Goals

(To be defined)

---

## Interface Signatures

This section defines the TypeScript types, interfaces, and JSON schemas for all data structures. Keep this section updated as the data model evolves.

### (Placeholder - to be populated during implementation)

Example structure:

- `.campaign` file format schema
- `.tome` file format schema
- `.ruleset` file format schema
- `.character` file format schema
- Entity schemas by type (see [shared-types.md](../shared-types.md) for `EntityType`)
- State snapshot format

---
